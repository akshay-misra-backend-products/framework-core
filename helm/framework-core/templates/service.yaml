apiVersion: v1
kind: Service
metadata:
  name: {{ include "framework-core.name" . }}
  labels:
    {{- include "framework-core.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "framework-core.selectorLabels" . | nindent 4 }}
